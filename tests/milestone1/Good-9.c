// To check the the structure of for loop:
#include<stdio.h>
void main() {
	int x = 0;
	for(;;) {
		if(x == 2);
		x++;
	}
}

object Demo {
   def main(args: Array[String]) {
      var x = 0;
      // An infinite loop.
      while( True ){
         if( x == 2 ){
            x = x + 1;
         }
      }
   }
}
